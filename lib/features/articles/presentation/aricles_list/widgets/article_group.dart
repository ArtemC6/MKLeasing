import 'package:flutter/material.dart';

class ArticleGroup extends StatelessWidget {
  const ArticleGroup({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}